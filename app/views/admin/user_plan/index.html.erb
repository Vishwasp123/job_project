<%= form_for @user_plan do |form| %>
  <%= form.collection_select :user_id, User.all, :id, :name %>
  <%= form.collection_select :plan_id, Plan.all, :id, :name %>
  <%= form.submit 'Purchase Plan' %>
<% end %>
